# Mock data

Mock data meant for usage in local tests, the files are meant as a one-to-one
mapping for the files in the `public/data` directory.

As the dataset grows this may be removed in the future.
